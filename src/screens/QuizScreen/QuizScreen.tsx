import { QuizStartCard } from '@features/quiz/components/QuizStartCard/QuizStartCard';
import { Container } from '@mui/material';
import { useNavigation } from 'react-router-dom';

export const QuizScreen = () => {
  return <QuizStartCard />;
};
